version: '3.9'
services:
  api:
    build: .
    command: python run_api.py
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
  bot:
    build: .
    command: python run_bot.py
    env_file:
      - .env
    volumes:
      - .:/app
